    $("#start-button").on("click", start);  // starts the games 
    $("#submit").on("click", finish);  // submits answers and finishes the game
    $("#restart").on("click", restart);  // restarts the games 
// ----------------------------------------------------------------
// functions
  // Hide the Question 
  // Have the instructions and Start Button Appear
  // After hitting start button make the questions appear and timer appear
  // Have the contestants answers be registered by the computer
  // Once the contestant is done have them hit the submit button
  // Once submit button is pressed have the guess checked against answer
  // Display how many were correct and how many were wrong
  // If timer runs out before all questions were answered end the game
  //
